# Taste

- Asked for a "night-neon" theme for the commit-graph lane palette: high-saturation electric hues (neon pink, electric cyan, neon violet, neon orange, neon green, hot red, neon yellow, electric blue, acid lime, neon mint) tuned to pop/glow against the dark background, explicitly kept distinct from the teal UI accent — prefers vibrant, neon-tinged graph colors on the dark theme rather than muted/subdued ones. Confidence: 0.6
- Clarifies terse UI requests with terse follow-ups and expects literal interpretation of the named target: when they said clicking "WorkingRow" should "highlight first row", they meant the WIP row itself should get the highlighted/selected state (like commit rows), not that the click should select/highlight a different row (e.g. the HEAD commit) — "first i mean WIP row it self". Reads requests literally rather than reinterpreting the target. Confidence: 0.6
- Prefers a plan/architecture-first workflow: when starting a new project, wants the assistant to propose architecture and a phased plan before writing code ("plan and sugest me the architecture"). The same applies to non-trivial feature changes: when the assistant was blocked by plan mode and offered to implement directly, the user replied "create plan first" — a written plan must be produced and approved before any code changes. Confidence: 0.9
- Values verifying against real-world data: when parsers/interfaces target external tools (e.g. git), checks the actual command output against a real repository before trusting fixtures. Confidence: 0.7
- Chose Tauri 2 + React + TypeScript (Rust backend) for a desktop GUI over Electron, and prefers shelling out to the real `git` CLI over git libraries (libgit2/isomorphic-git). Confidence: 0.7
- Prefers custom, dependency-light implementations for core rendering logic (hand-rolled canvas graph + virtualized rows + pure, unit-tested layout algorithm) over pulling in a graph library. Confidence: 0.7
- Prefers native, platform-standard dialogs (official Tauri plugin-dialog with a directory picker) over web-view hacks (e.g. `webkitdirectory` file inputs) for file/folder selection — a "Browse" button must open a native folder picker, not a file chooser. Confidence: 0.6
- Chose full-feature scope (merge/rebase/cherry-pick/stash UI) over a minimal v1 when building a git client — prefers complete workflows over MVP cut-downs. Confidence: 0.6
- Dislikes silent failures: reports bugs like "nothing happen" after an action and expects UI actions to either work or surface a visible error (e.g. alert) rather than failing silently (reported "modified file not showing any changes in diff-viewer" when the diff silently returned empty hunks for untracked files; reported "click on WIP row not happen anything (CommitComposer not showing)" when a refactor dropped the composer-open behavior). Confidence: 0.8
- Iterative hands-on tester: user runs the app themselves after each fix and reports back remaining issues in terse, symptom-focused batches ("graph drawing still wrong", "selecting commit not showing the detail on sidepanel", "only new file colored, modified and deleted still has no color", "the scroll not following highlighted row"), often attaching a screenshot to illustrate the GUI symptom, and expects the assistant to address all listed issues in the next round rather than stopping at the first fix; retests each claimed fix in the running app and reports terse failures like "still apear" when the fix didn't hold. Confidence: 0.8
- Prefers smooth, rounded curves over sharp right angles in rendered visualizations (asked graph connector lines to curve instead of a "90deg L shape"); favors polished, natural-looking geometry (e.g. rounded corners proportional to lane width) once the core logic works. Confidence: 0.6
- Prefers UI consistency: when a new surface duplicates a pattern that already exists in the app, wants it to reuse that same component/look rather than a new ad-hoc layout — e.g. asked the composer's Unstaged/Staged lists to "look like file tree, similar to CommitDetail's file tree", prompting the tree builder + FileTree to be generalized into shared/. Fresh instance: for the diff minimap, suggested "if able to place it inside ScrollArea.Scrollbar will be better" — prefers hosting new UI inside the existing design-system primitive (BaseUI's Scrollbar track) over a standalone overlay strip stacked next to it, so the new surface inherits the system's scrollbar behavior/look rather than duplicating chrome. Confidence: 0.85
- Prefers designating a single canonical component for a UI primitive and migrating the whole app to it (rather than letting each surface roll its own): asked to "migrate all scrollable to BaseUI's ScrollArea for consistent scrollbar look" — wants every scrollable region (file trees, commit list, detail panels, log output, etc.) to go through the shared `ScrollArea` wrapper instead of native CSS `overflow` rules, so the scrollbar appearance stays uniform across features. Confidence: 0.7
- Prefers compact forms with minimal chrome: field placeholders act as the titles/labels (no separate visible `<label>`), inputs sized compactly, and message fields placed adjacent to the primary action (Subject/Description at the bottom, just above the Commit button). Confidence: 0.7
- Prefers UI panels/lists to fill the available space rather than sit at content height leaving dead space — explicitly asked both staging file trees to "stretch (flex-grow)" to fill the pane. Confidence: 0.6
- Directs UI changes in terse, implementation-level terms using technical vocabulary (e.g. CSS property names like "flex-grow", class selectors like ".diff-col stretch to bottom", CSS position terms like "sticky right") and expects them applied directly rather than re-interpreted; often pins the exact file/component (e.g. "click on WorkingRow, should highlight first row", "make stage/unstage button inside FileTree sticky right") and lets the assistant decide the mechanics. Fresh instances: ".diff-gutter not enought space for 3digit+ number. make it enought for 6digit number" (bare class name + concrete spec) and the FileTree sticky-right request (target + desired outcome stated in a few words, mechanics left to the assistant). Confidence: 0.8
- Expects fine-grained interactive affordances on UI controls: clickable icon buttons must show `cursor: pointer`, be vertically centered within their container, and use the design system's ghost-button styling (shared `.icon-btn` primitive) rather than a bare default button — reported in terse CSS terms (".diff-close not center vertically, missing cursor-pointer, also make it ghost button"). Confidence: 0.6
- Does not want the assistant to auto-commit work — commits should happen only when the user explicitly asks ("dont immediately commit this project unless i ask you to commit"). Confidence: 0.9
- Wants core project documentation before going further on a project: asked for a README.md (what it is, features, prerequisites, setup, scripts, architecture) and a ROADMAP.md (phase-based tracker of done/in-progress/planned work) to be in place early and reflect the current state; continues to actively use the ROADMAP as the living tracker for planned features (e.g. asked to add an AI integration phase covering generate commit message, compose/group commits by change context, and generate PR). Confidence: 0.8
- When choosing a project name, values names that don't collide with existing products/trademarks — rejected "GitLens" (Axosoft's product) and "GitTree" (already taken) before settling on "GitCanoe" (since renamed to "GiTako", 2026-08-28). Confidence: 0.4
- Prefers full-file context in diff views: wants the entire old and new file rendered side-by-side with changed regions highlighted, not just the changed hunks — with guards so very large files or high change density fall back to a compact hunk-only view or a "file too large" notice. Confidence: 0.6
- For side-by-side diffs, both panes (old vs new) must always be visibly present: added files show an empty/placeholder old side with all content as additions on the new side, deleted files show content on the old side with an empty/placeholder new side — never mirror content onto both sides or let one column appear blank/collapsed, since a side-by-side viewer is expected to "show 2 contents, old vs new" ("diff viewer side-by-side should be showing 2 contents, old vs new. currently if open added file. if show only new content. on deleted file too"). Followed up ("still not working, i think it caused by HunkView fallback") when an alternate render path still produced a single column — the requirement must hold on every render path, not just the main one. Confidence: 0.9
- Prefers wide layouts for diff viewing: a side-by-side diff should replace the full left panel (the commit graph) to maximize horizontal space rather than squeeze into the narrower right pane. Confidence: 0.5
- Prefers compact close controls in full-pane views: an "X" button at the top-right to close, over a labeled "← Back" button. Confidence: 0.4
- Prefers small utility functions collected into a dedicated folder rather than scattered inline in components ("refactor all small utils function into folder src/utils, more organizer"). Confidence: 0.6
- Keeps an eye on file size and proactively asks to refactor files that "start became fat" — flagged the single global CSS file and accepted splitting it by feature area (base/repo/workspace/commit-list/detail/file-tree/composer/diff, matching the feature folders) with the original file kept as an entry that `@import`s the parts, so each area's styles land in a dedicated file. Confidence: 0.6
- Wants every git state handled correctly, including edge cases: unstaged/untracked (new) files, staged-only changes (index vs HEAD), and root commits — expects the diff tool to synthesize/show changes for untracked files rather than showing nothing because `git diff` ignores them. Untracked (`??`) files must also get a proper colored status in UI lists (rendered as green "added"/plus like other new files) rather than a colorless/gray unknown state. Confidence: 0.7
- Wants errors surfaced in the UI instead of swallowed: when a command fails, the diff view should display the actual error message rather than a permanent "Loading…" or an empty result. Confidence: 0.7
- Prefers full content over truncation in code/diff views: long lines should expand the row and trigger horizontal scroll (max-content columns, `white-space: pre`) rather than being clamped to the container and ellipsized ("need horizontal scroll for DiffView"). Confidence: 0.5
- Prefers the side-by-side diff to behave like two text-editor panes, not one monolithic scrollable table. Final model: two independent ScrollAreas side-by-side with BOTH vertical and horizontal scroll synced between them — vertical keeps rows aligned, horizontal keeps the offset matched, while each pane is still its own scroll container with `min-width: max-content` content so long lines clip cleanly at the pane edge instead of bleeding into the sibling. History of reversals: first asked for synced horizontal scroll ("make both .diff-col sync scroll"), then reversed to independent per-pane when the shared/synced approach let OLD text overlap into NEW ("wait, it's totally not working... i think easier to use independent per pane"), then reversed back to synced ("horizontal scroll not synced between 2 pane") — the accepted solution mirrors both `scrollTop` and `scrollLeft` to the sibling via a re-entrancy guard while keeping per-pane clipping. Rejected the single-table horizontal scroll and per-row scrollbars. Line-number gutters pinned (sticky) while line content scrolls. Confidence: 0.9
- Wants project conventions (folder structure, import rules) codified in AGENTS.md project memory so agents follow them consistently; asked "how ensure Agent keep the rules?" and approved drafting AGENTS.md as the mechanism. Confidence: 0.6
- Prefers mutually-exclusive controls collapsed into a single toggle that flips label/state rather than showing both buttons at once (asked to make "Expand all"/"Collapse all" a single toggled button). Confidence: 0.5
- Prefers compact status summaries: hide UI rows when there is nothing to show, and use per-type icon + count badges (e.g. "WIP" with modified/added/deleted counts) over verbose wording ("hide if Working Dir clean, update wording to WIP with [icon] [number of files]"). Confidence: 0.5
- Wants syntax highlighting for known file types in code/diff views, with a plain-text fallback for unknown extensions ("i think i need syntax highlighter for known extentions"). Confidence: 0.5
- Wants visual layers to compose rather than replace: when the word-change tint was added to changed diff lines, explicitly asked "is possible to keep syntac highlighter while add changes tint?" — shiki token colors must be preserved on changed lines even where the change-word background overlays, not traded off for the tint (the assistant merged the two partitionings of the same line by character position so each span carries both a token color and a `changed` flag, with a plain-text fallback while highlighting is async-loading). Expects syntax highlighting and change emphasis to coexist on the same rendered content. Confidence: 0.7
- Expects syntax highlighting to apply to EVERY diff line, including standalone added/removed rows that have no counterpart to word-diff against: reported "some hunk still has no syntax highlight. like composer.module.css L92-102" (terse report anchored to a specific file + line range for reproduction) when a pure-addition block rendered as plain text because the change-cell early-returned when `other == null`. The fix renders shiki tokens for standalone changes too — syntax colors preserved, so highlighting must never be dropped just because there's no paired line to diff against. A follow-up on a brand-new file ("new file like `wordDiff.test.ts` show indentical both OLD n NEW. it should show empty on OLD side, and all + in NEW side") refined this further: standalone change rows must ALSO carry the whole-line change emphasis (`changed` tint on every token, not `changed: false`), so a new file's NEW side reads as all additions (GitHub-style new-file view) while the OLD side stays empty — never mirroring content onto both sides. Confidence: 0.7
- Wants scrollbars/controls always visible and reachable without scrolling first: flagged the diff's horizontal scrollbar only appearing after scrolling to the bottom as "not very UX friendly" and accepted an always-visible scrollbar strip pinned at the bottom of the view (detached from the content, driving both columns' horizontal scroll). Confidence: 0.7
- Rejects throwaway/placeholder-quality UI: called a minimal welcome screen "not very usefull, look stupid" and asked for a reference-grade screen (JetBrains-style Repositories view with search + recent list) instead. Confidence: 0.5
- Prefers `clsx` for conditional/dynamic class names instead of template-literal string concatenation in `className` — explicitly asked to "install and use clsx for dynamic classes" and expects it applied across all components with dynamic classes (rows, arrows, diff lines), not just the file at hand. Confidence: 0.9
- Cares about test coverage quality and pushes toward 100% but accepts pragmatic limits: asked "is possible to achive 100%" and accepted the conclusion that 100% is unreachable without removing defensive branches (e.g. `if (!container) return` guards) or shipping a full canvas polyfill — values extracting testable pure logic (e.g. `drawGraph` factored out of `GraphCanvas` so the drawing math can be unit-tested with a mocked 2D context) over leaving canvas code untested, and excludes `*.module.css` + barrel `index.ts` re-exports from coverage as untestable surface area rather than chasing false 100%. Confidence: 0.85
- When components or hooks need coverage but have hard-to-test side effects (canvas rendering, ResizeObserver, getContext, etc.), prefers extracting the core logic into a pure function in a separate file and importing it back into the component, so the logic is mockable from a test environment where the original DOM/canvas API is unavailable. Confidence: 0.7
- When a tricky UX problem resists iteration, prefers to park it as a tracked follow-up item on the ROADMAP rather than keep polishing ("hmmm... still has poor UX, for now add this edge case to ROADMAP") — accepts the current implementation as-is and defers the redesign to a later roadmap item, rather than blocking on it. Confidence: 0.7
- Expects exclusive selection highlighting in lists: clicking the WIP row must clear any previously selected commit (`select(null)`) so exactly one row is highlighted at a time — reported "now there 2 highligthed rows after click WIP row" when the WIP row and the previously selected commit both stayed highlighted, and followed up with "double highlight still happened" when a partial fix (clearing DOM selection only) left both highlights — so the invariant must hold across all render surfaces, enforced by making the selection state mutually exclusive (e.g. stored centrally so `select(hash)` clears WIP selection and vice versa). Confidence: 0.8
- Expects selection/highlight state to be visually mirrored across every surface that renders the same item: after the WIP row highlight was fixed in the list, immediately reported "on GraphCanvas still not hightlight wip node" — the graph canvas node must draw the same selected ring as the row, using the same visual treatment as commit selection (white ring), so row list and canvas graph stay in sync. Confidence: 0.6
- Expects selection highlight to extend continuously edge-to-edge across adjacent visual regions with no background gaps: when the selected commit row's highlight stopped at the right edge of the graph band and left a visible gap before the ref-badge column on the left, asked to "extend highlighted row to left of container" — every sub-region that belongs to the selected row (tag/ref-badge cell, graph band, text columns) must share the selected background so the highlight reads as a single unbroken band, not a band with a gap at internal column boundaries. Reiterated "still not work, what i need is full row highlight" when the first fix only tinted the tag/ref-badge cell — the graph band and every intermediate region must be included too, so the highlight spans from the container's left edge across the entire row. Confidence: 0.85
- Project renamed from GitCanoe to GiTako (2026-08-28): display name is "GiTako" (capital T), while package/identifier names are lowercase "gitako" — npm package name, cargo crate "gitako"/"gitako_lib", Tauri identifier `dev.gitako.app`, binary `./target/debug/gitako`, and localStorage key prefix `gitako.`. Use these exact casings in code, configs, and docs; don't refer to the project as GitCanoe anymore. Confidence: 1.0
- Dev machine runs Ubuntu 24.04 and sudo requires a password (`sudo -n` fails), so the assistant cannot run apt installs itself — prefer user-local installs (rustup to ~/.cargo, local node_modules) and hand the user the exact sudo command to run when system packages are needed (Tauri 2 stack on Ubuntu 24.04: pkg-config, libwebkit2gtk-4.1-dev, libssl-dev, libxdo-dev). Confidence: 0.8
- The agent shell is plain POSIX sh — `source` is unavailable; use `export PATH="$HOME/.cargo/bin:$PATH"` or absolute binary paths instead of `source ~/.cargo/env`. Confidence: 0.8
- Rust toolchain is installed user-locally via rustup (stable, minimal profile) in ~/.cargo/bin and is not on the default PATH in agent shells. Confidence: 0.7
- Node devDependencies on this machine must be installed with `NODE_ENV=development npm ci` — a plain install skipped them, leaving node_modules without tsc/vitest. Confidence: 0.6
- npm postinstall scripts are gated by an allow-scripts policy: esbuild's install script was blocked (which can break Vite); fix with `npm install-scripts approve <pkg>@<version>` then `npm rebuild <pkg>`, and verify with `<pkg> --version`. Confidence: 0.7
- Launch the GiTako desktop app in dev mode with `npm run dev:desktop` (Vite dev server on port 1420 + cargo build of the Tauri backend, binary `target/debug/gitako`); verified working 2026-08-28 — cold first build ~2 min, cached relaunch ~4 s. Confidence: 0.7
- Approves of the assistant's caution against `curl ... | bash` style remote-script installers — accepted the safer path (verify on npm first, then install via `npm i -D` or use the package's documented manual path) rather than running the README's one-liner, even for a plausibly legitimate package. Expects the assistant to flag and question remote-script installers before executing. Confidence: 0.85
- When a package/tool asks for a major toolchain swap (e.g. Vite 5 → vite-plus core, Vitest 2 → 4), prefers the full documented migration (registry overrides, config imports updated to the new package) over a half-installed devDep that wouldn't actually wire the tool up. Confidence: 0.75
- Project toolchain (as of 2026-08-28) is on `vite-plus@0.3.0` (the voidzero-dev unified toolchain): `vite.config.ts` imports `defineConfig` from `"vite-plus"`, `vite-plus-core` and `vite-plus` are devDeps, `vitest` is pinned to `4.1.11` via `overrides`, the `vp` CLI is available in `node_modules/.bin` alongside `oxlint`/`oxfmt`. The project no longer lists `vite` or `vitest` as direct devDeps. Confidence: 0.7
- After toolchain swaps, expects verification end-to-end before declaring done: run `tsc --noEmit`, run `vitest run`, and actually launch `npm run dev:desktop` to confirm the app starts — not just an `npm install` success. Confidence: 0.8
- When running `npm install` doesn't apply package.json edits as expected, expects the assistant to re-read the file (an unrelated `allowScripts` block may have been added concurrently) and retry, not assume the install was successful. Confidence: 0.6
- When unexpected files appear modified in the working tree (large `git status` with many unfamiliar files, `devEngines` blocks, version changes, etc.), the user wants the assistant to STOP and ask before touching them, treating them as in-progress work. Chose "Leave my changes; do not touch the rest" and reinforced with "leave the rest alone" — expects the assistant to scope changes narrowly to the asked task and surface unfamiliar state via `git status` / `git diff` before deciding what to modify, even if the originally-requested fix requires changing one of those files. The narrowest-scope edit should be made only after confirming which files are safe to touch. Confidence: 0.9
- Reports commit-graph rendering bugs in terse terms — either as a direction sequence (e.g. "some node that 'go up, turn right'") or by naming shape types ("fork node", "Merge node type A/B") — anchoring the symptom to a specific commit hash and repo ("example on commit `0d47474` in `ghoulscript` project") so the assistant can locate and reproduce the exact spot. Confidence: 0.65
- Prefers graph edge lines drawn in a single consistent color with no visible color seam where two segments join (flagged a Type A merge edge's "mix line color that should haven't", wanting the whole edge rendered in one stroke/color). Confidence: 0.6
- Prefers user-adjustable UI: asked for resizable columns in the commit list ("is possible add resizable cols? so user can freely choose commit message or graph") — but corrected the design so the GRAPH band stays fixed (auto-sized to the lane count) and only the commit-message column is resizable via the draggable divider ("graph should not moveable. only commit message that should resized"). Confidence: 0.7
- Prefers text columns that overlay the canvas-drawn commit graph to have a solid background color (e.g. `var(--bg)`) so graph lines don't bleed through the message text — content areas drawn on top of canvas graphics must be opaque. Confidence: 0.7
- Expects a clean React console and treats hook-order violations as must-fix bugs in any build: pasted the full "React has detected a change in the order of Hooks" dev warning when a feature introduced a `useRef` below an early return, and later pasted the minified production error #310 ("Rendered more hooks than during the previous render") for the same root cause (`useCallback` declared after an early return). Expects the fix to hoist all hooks above the early return — routing computed values through refs (e.g. `textLeftRef`) so hook count/order is stable across renders — rather than ignoring or working around it. Confidence: 0.7
- Interested in a two-tier model workflow: plan with the current (stronger) model, then execute the approved plan with a faster/cheaper model — asked "is possible to plan with current model and execute with deepseek-v4-flash?", then confirmed "yes" to proceed once the switch path (write plan, `/model` switch mid-session to `deepseek/deepseek-v4-flash`, then approve) was explained — a real preference for splitting expensive planning from cheap execution, not just curiosity. Confidence: 0.7
- When executing an already-approved plan, wants direct execution without re-presentation: explicitly instructed "do NOT enter plan mode and do NOT call exit_plan_mode to re-present it" — the assistant should read the plan file first, then implement it step by step, referring back to the plan as it works; approval is final and should not be re-confirmed or re-shown. Confidence: 0.9
- Prefers color palettes ordered logically by hue rather than in an arbitrary/curated order: after receiving the night-neon lane palette, asked to "sort the color by their hue" — expects the palette array to ascend around the color wheel (orange → yellow → lime → green → mint → cyan → blue → violet → pink → red), with each entry annotated with its hue degree, so consecutive lanes progress smoothly through the spectrum. Confidence: 0.6
- When refining a color palette, specifies the desired color count plus a start/end color range and expects the palette rebuilt as an evenly hue-spaced arc interpolating between those endpoints, not just hue-sorted discrete colors: "make it 15 colors, start from pinkish purple -> neon purple -> neon blue -> neon green" was implemented as a smooth ~320° → ~142° sweep through 15 colors, each annotated with its hue degree — treats the palette as a continuous gradient across the spectrum. Confidence: 0.6
- Prioritizes adjacent lane colors being visually distinguishable over keeping the palette within a requested hue range: flagged "distance between each color look too short. hard to distinguish" when 15 colors were squeezed into a ~178° arc (~13° apart) and accepted spreading them across the full 360° wheel (~24° apart, ~2x separation) even though that broke the earlier pinkish-purple→neon-green arc — when the two goals conflict, discriminability wins, and the user is open to the assistant proposing the fix ("any idea"). Confidence: 0.5
- When asking to revert a change (e.g. "revert back to 10 colors"), still expects the explicitly-requested customizations from the newer version to be kept ("but keep sequence pinkish purple → neon purple → neon blue → neon green") — reverts are selective: undo only what the user objected to (the color count/spacing), preserve the parts they explicitly asked for (the opening sequence), and rebuild the rest around them. Confidence: 0.6
- Wants elements attached to a commit to carry that commit's lane color so color acts as a visual grouping cue: "make commit-ref-badge match color with their node color" — ref badges in both the commit list and the commit-detail pane tint to the commit's lane color (translucent tinted background/border + full-color text), rather than a fixed accent color, so badges read as belonging to their node. Confidence: 0.6
- Prefers the currently-checked-out branch (HEAD, `kind: "head"`) to be clearly and prominently distinguished in ref badges: a checked (`Check`) icon on the left side plus a filled, lane-tinted badge instead of the plain transparent border-only style. Keeps pushing the fill strength up until it reads as active — 14% and 25% transparent tints were each called out as not looking focused enough ("still not very look focusable"), and the accepted treatment was 38% of the lane color mixed against the solid background. Confidence: 0.75
- For tinted fills on the dark theme, prefers mixing the color against the solid background token (`var(--bg)`) rather than against `transparent`: transparent-tinted overlays read as too subtle/faint, while mixing against the solid background produces a noticeably more visible and consistent fill (demonstrated when the active-ref badge tint was changed from `color-mix(..., transparent)` to `color-mix(..., var(--bg))` and the user accepted it without further pushback). Confidence: 0.6
- Prioritizes text contrast/legibility on colored and tinted surfaces: when badge text was rendered in the full neon lane color, called out "the text has very bad contrast, i think white will better" — prefers white (`#fff`) text over saturated/colored text whenever the background is a bright or dark-tinted fill, so text stays readable on both lane-tinted and fallback backgrounds. Confidence: 0.7
- Prefers design-system tokens over hardcoded color values, and soft off-whites over stark pure white: after badge text was set to `#fff`, asked "any alternative instead of pure white?" and accepted the app's primary text token `var(--text)` (#e3e8ee) — same contrast on the dark tints but softer and consistent with the rest of the UI instead of "glowing pure white". Reaches for existing theme tokens/colors rather than hardcoded literals when polishing UI. Confidence: 0.6
- Wants the neon identity to extend beyond the graph lanes into the app's primary UI accent: explicitly chose `#ff5ec8` (neon pink) as `--accent`, replacing the teal accent that had previously been kept distinct from the lane palette — the accent drives buttons, focus rings, selection tint, WIP icons, the dirty pill, and the ref-badge fallback, unifying UI chrome with the night-neon graph theme. Accepted the assistant's derived-token shift required for a light neon accent (dark `--on-accent` text, pink-tinted dim/border/selected) without objection. Confidence: 0.7
- Errors in user-facing async flows (open-dialog, repoRoot, file picker) must produce a visible error banner with `role="alert"` describing the failed operation + the underlying error message (e.g. "Cannot open repository: not a git repo", "Failed to open picker: plugin crashed") — never silent failure, and never a raw stack trace; the banner message prefixes the action that failed so the user can tell what went wrong. Confidence: 0.7
- For dialogs/pickers that may be cancelled, treats a null return as a no-op (don't surface it as an error) and only surfaces thrown errors — the user pressing Cancel is a normal flow, not a failure. Confidence: 0.6
- Issues terse, file-anchored feature requests that pin the target file but leave UX details (key bindings, edge-case semantics, scope) entirely to the assistant — "@src/features/commit-graph/CommitList.tsx add keyboard navigation (arrow keys)" produced ArrowUp/Down + PageUp/Down + Home/End + auto-scroll-into-view + WIP-row composer-open behavior with no further specification. The assistant is expected to make these UX decisions (consistent with surrounding click semantics), then verify with typecheck + tests. Fresh instance: "@src/features/commit-graph/refBadge.tsx add checked icon on left side on current active branch also make badge a litle less transparent" — file pinned, desired outcome stated in a few words, mechanics (which kind marks HEAD, how to style, whether grouped badges are covered) left to the assistant. Confidence: 0.8
- When a test fails, prefers fixing the test to match real product semantics rather than changing the product to satisfy the test: in the keyboard-nav task, a failing test expected "ArrowDown selects the newest commit" but the actual behavior (matching the existing click semantics: "ArrowDown onto row 0 = WIP row") was correct — so the test was rewritten to assert the real behavior, then re-verified. Treats test assertions as potentially-wrong, not just product code. Confidence: 0.7
- Before implementing a non-trivial feature change, reads the surrounding context thoroughly: component file, related store/state, layout/render constants, the CSS module, and the existing test file — to match existing patterns (hook order, helper functions, testing conventions) before writing new code. Confidence: 0.7
- Prefers larger, readable font sizes for compact UI text: called the ref badge's 11px (`--fs-xs`) label "very small" and asked to raise it to 14px — treats small design-token sizes as too small for on-screen badge/label text and expects the concrete larger size applied (icons scaled up to match). When text still looks too small, prefers raising the whole global font scale rather than bumping individual elements ("i think what i want is raise all font size little high a bit") — implemented by bumping the `--fs-*` design tokens in base.css ~1px per step (11/12/13/14/16 → 12/13/14/15/17) so every surface scales up consistently, with any hardcoded font sizes converted back to the tokens; a modest bump ("a bit"), not a drastic one. Confidence: 0.7
- Once a global/token-level fix supersedes an earlier one-off element override, expects the one-off override to be reverted rather than layered on top: after the whole `--fs-*` scale was raised, explicitly reminded "dont forget to revert back ref badge size" — the badge's interim 14px text/13px icons had to go back to token-relative `--fs-xs` + 11px icons so the change is applied once, at the global level, with interim hacks cleaned up. Also expects the assistant to remember such cleanup follow-ups without being asked twice. Confidence: 0.7
- Prefers panes NOT to be wrapped in a root-level ScrollArea when inner regions already scroll internally: after the CommitDetail pane was converted to a fixed flex column (header/meta at natural height, inner FileTree scrolling via `flex: 1; min-height: 0`), explicitly asked "remove scrollarea on the root of component" for the CommitComposer too — the composer became a plain flex column with the two staging FileTrees sharing the space and scrolling internally, the form pinned at the bottom, and the now-orphaned `detailPaneViewport` / `composerContent` scroll-wrapper classes deleted. Avoids nested scroll containers / duplicate scrollbars on one surface; the FileTree's internal ScrollArea is the single scroller. Confidence: 0.8
- When a structural/layout refactor is applied to one pane, expects the same change to be propagated to sibling components sharing the same pane base: right after the CommitDetail pane lost its root ScrollArea, asked "@src/features/commit/CommitComposer.tsx too, should FileTree should expanded" and then "remove scrollarea on the root of component" — the composer (which shares the `.detailPane` base) was expected to adopt the identical structure, not keep its own divergent layout. Treats shared-base components as needing consistent architecture, and also expects dead code from the refactor (unused imports, orphaned CSS classes like `detailPaneViewport`) to be cleaned up. Confidence: 0.7
- Wants layout to stay stable when UI elements conditionally show/hide: asked to "set height on label UNSTAGED/STAGED so if button show/hide, it's not have layout shift" — when a control (e.g. the "Stage all"/"Unstage all" button) appears or disappears based on state, the container must reserve fixed space (fixed header height, no padding-dependent line-box growth) so toggling the control doesn't shift the layout vertically. Confidence: 0.7
- Extends the vibrant/colorful aesthetic beyond the graph lanes to interactive action controls: asked to "add color to Stage, Unstage, Stage All, Unstaged to make it not very boring" — the staging buttons/titles all in the same dim `--text-dim` read as monotonous, and the user expects action labels/buttons to carry color (e.g. semantic accent tokens) so interactive elements stand out from passive text instead of a uniform gray UI. The coloring must cover EVERY staging control — per-file buttons AND the bulk "Stage all"/"Unstage all" buttons — when the assistant declared the work done but the bulk buttons weren't visibly colored, the user caught it with a terse "you forget Stage All n Unstage All button" follow-up. Confidence: 0.7

- Expects claimed UI styling changes to actually be visible in the running app: automated tests don't validate CSS, so the assistant must verify selectors against the real DOM structure before declaring a styling task done — the previous "colored the staging actions" claim was wrong because `:first-of-type`/`:last-of-type` selectors silently matched nothing (`.composerHead` and `.composerForm` are interleaved `div` siblings, so the sections aren't first/last of type), leaving titles and bulk buttons uncolored until the user spotted it; the fix replaced fragile pseudo-class position selectors with explicit modifier classes (`.stagingUnstaged` / `.stagingStaged`) and propagated the same correction to the border-removal rule. The pattern recurred when "remove bg color for '+'/'-'" was declared done but the sign still showed a background because the `.add`/`.remove` class remained on the sign spans — the user caught it with a terse "it still have bg caused by .add & .remove". Confidence: 0.7
  on "sticky right" — a long filename must truncate (ellipsis) rather than push the `margin-left: auto` action off-screen. The accepted fix was `min-width: 0` on the truncating name (a flex child with `nowrap` + `overflow: hidden` refuses to shrink below its content width without it, shoving the action button past the viewport edge) plus `position: sticky; right: 4px` on the action itself as extra insurance. Confidence: 0.7
- Prefers repeating background patterns to stay visually continuous across adjacent rows: when per-row elements each carry their own `repeating-linear-gradient` (e.g. the diagonal stripes on empty diff cells), the stripes must read as one seamless pattern spanning the whole column, not disconnected segments that restart at each row's top-left corner — reported "using 45deg make stripe not connected with row bellow it". The first fix passed the row index as a CSS variable (`--row-i`) and offset each cell's `background-position` by the accumulated row height (`calc(-1 * var(--row-i) * 1.5em)`), but the user found that approach "look weird" and asked to change the angle instead. Confidence: 0.6
- Prefers achieving seamless visual effects by construction rather than by per-element offsets: when the `--row-i` background-position hack looked odd, the user asked "better change the degres, row height always same right?" — confirming rows are uniform (`line-height: 1.5`, no wrapping) and steering the fix to geometry: a 30° angle with a 0.75em axis period makes the pattern's vertical period exactly one row height (0.75em / sin(30°) = 1.5em), so every row starts at the same phase and the stripes tile seamlessly with zero per-row offsets; the inline `--row-i` styles were removed entirely, leaving the CSS self-contained. Prefers simpler invariant-based solutions over inline-style workarounds. Confidence: 0.65
- Once a plan is written and approved, expects the assistant to execute it step by step (writing the code in plan order, following the plan's file structure and design decisions, running the plan's verification commands) rather than improvising: the diff full-row-background + word-highlight task was executed exactly in plan order — new `wordDiff.ts` utility + tests first, then component wiring, CSS, component tests, then the plan's `tsc --noEmit` + `vitest run src/features/diff` verification, then a full-suite run before declaring done. Confidence: 0.6
- When a new pure utility is introduced, prefers it feature-local (colocated in the feature folder) rather than in shared/ when only that feature uses it, and expects unit tests to be written alongside it from the start (the diff word-diff utility shipped with its own test file in the same folder). Confidence: 0.6
- When a test fails during implementation, treats the failing test expectation as the likely source of error (not the product code) and verifies the product's actual semantics before rewriting the assertion: in the word-diff task, three rounds of test failures were all traced to test expectations contradicting the real token model (whitespace kept as standalone tokens, `\s+` runs merging, merged adjacent changed runs) — the tests were rewritten to assert the actual correct behavior, never the product. Confidence: 0.6
- Wants the change-tint to read as one continuous block across the diff row — but only in the code area: the add/remove tint covers the full row width and latent bugs found along the way (hover washing out the tint due to specificity) are fixed in the same pass. The sticky line-number gutter is the explicit exception: after initially asking for the gutter to match the row tint (with green/red line numbers), the user reversed that decision ("hmm... i take my decision for line number bg color") and chose a fixed neutral gutter — the gutter keeps a distinct neutral `var(--bg)` on every row, line numbers stay the standard faint color on all rows (no green/red), and only the code area carries the green/red tint. A decision reversal like this is expected to be applied cleanly (both the gutter tint AND the colored numbers reverted), not just partially. The same de-coloring extends to the "+"/"-" sign indicators: asked to "remove bg color for '+'/'-'" — the sign glyphs carry no background box, but the sign TEXT color must stay green/red. When the assistant removed the color class entirely, the user corrected with a terse "but text color should still" — so "remove bg color" means background-only, not dropping the semantic color cue; the shared `.add`/`.remove` rules were scoped per element (`.diffLine.add` row tint, `.diffSign.add` sign text color, `.diffWord.add` word highlight) so removing one surface's background never kills another surface's color. The green/red change tint therefore appears only on the code area and on the sign glyphs, never on the gutter or line numbers. Also asked to "add little gap spacing between line num and +/-" — expects small breathing room between adjacent diff-chrome elements (implemented as a 6px gap between the line number and the sign) so the neutral chrome reads cleanly. Confidence: 0.75
- For diff rows, when full-row change tints are applied, expects the hover background to be scoped to context rows only (`:not(.add):not(.remove)`) so hovering can't wash out the add/remove tint — a specificity interaction the assistant discovered and fixed proactively, and the user accepted. Confidence: 0.5

- For diff rows, when full-row change tints are applied, expects the hover background to be scoped to context rows only (`:not(.add):not(.remove)`) so hovering can't wash out the add/remove tint — a specificity interaction the assistant discovered and fixed proactively, and the user accepted. Confidence: 0.5

- Before implementing a roadmap/backlog item, wants to confirm the item's actual purpose and whether existing UI already covers it, to avoid over-building: when asked to "continue Roadmap Phase 3", the user stopped the already-started implementation with "wait a minute, what is File content preview for?" and then "i think that's irrelevant for now, as DiffView already cover it", having the new preview code reverted — a feature that existing code already provides is treated as out of scope rather than polished further. Confidence: 0.7

- Treats the ROADMAP as a living tracker to be actively pruned, not just appended to: asked to "delete from ROADMAP" an item already covered by the DiffView and to "remove others irrelevant" (cancelled and superseded items), so stale, cancelled, or already-covered items are deleted rather than left as stale unchecked boxes — the roadmap should reflect only genuinely remaining work. Confidence: 0.7

- For components whose behavior depends on scroll geometry, this repo's test env (happy-dom) reports `scrollHeight`/`clientHeight`/`scrollTop` as 0 by default, so component tests must stub the geometry with `Object.defineProperty` on the viewport element after render and then force the component's metrics to re-read (e.g. dispatch a scroll event); and any dispatched DOM event that triggers a React state update must be wrapped in `act()` from `@testing-library/react` so the re-render flushes before assertions — without `act()`, React batches the update and queries run against a stale render, making the component appear not to render at all. Confidence: 0.6
- When a CSS-based fix is based on a guess about a library's internals and the user reports it still fails, expects the assistant to verify the real mechanism by reading the dependency's actual source in `node_modules` (component files, state-attribute mappings, state-update logic) rather than re-guessing or layering another workaround — the first scrollbar fix relied on an assumed `visibility: hidden` overflow signal that a source trace proved wrong (it was only a pre-measurement flag), and the correct fix came from reading `ScrollAreaViewport.js`'s `getHiddenState` and `ScrollAreaScrollbar.js`'s `stateAttributesMapping` to find the authoritative `data-has-overflow-*` attribute. Confidence: 0.8
- Expects scrollbar tracks to disappear entirely when content doesn't overflow: reported "ScrollArea still show scrollbar even it cannot be scrolled, why?" when a `keepMounted` ScrollArea kept a visible 8px track as an empty gutter on a non-scrollable region — an empty scrollbar gutter is treated as a bug, not acceptable chrome. The accepted fix mirrored Base UI's own thumb-hiding (inline `visibility: hidden` on the thumb when there's no overflow) onto the track via global `:has()` rules (`.scrollbarTrack:has(.scrollbarThumb[style*="visibility: hidden"])`), applied to both the app-wide scrollbar and the diff's track — hide the whole track whenever its thumb is hidden, so a short file never shows an empty gutter, and both reappear when content scrolls. Confidence: 0.7

- Prefers dropdown/overlay content to be "teleported" into a React portal at `document.body` rather than fighting the layout's `overflow: hidden`, stacking-context, and z-index constraints — explicitly suggested "i think you need teleporting the dropdown element" after several CSS workarounds (clip-path, hover z-index bumps, overflow juggling) failed to keep a hover dropdown from being clipped by an absolutely-positioned/scrollable ancestor. The accepted solution renders the dropdown via `createPortal` to `document.body`, positions it from the anchor's `getBoundingClientRect()` (with a small open/close delay + scroll/resize re-anchoring), and carries the anchor's lane color in via a custom property passed to the portal (since the portal can't inherit it). Prefers this clean mechanism over layering more CSS hacks. Confidence: 0.8
- Prefers aggressive overflow collapse combined with accurate grouping in badges: ref badges should show a single visible item and collapse everything else into a `+N` overflow chip (e.g. `[development][+1]`), where the count reflects the number of distinct items the dropdown will reveal — a local branch + its matching `origin/...` remote-here tracking pair counts as ONE item (a single row with both icons, labelled by just the shared base name), while genuinely distinct refs each count as their own item. Rejected both "show 2 visible" and "count every raw ref separately". Confidence: 0.75
- Prefers the overflow/aggregate chip's styling to stay consistent with the items it hides: the `+N` overflow chip (and the dropdown rows it reveals) should carry the same lane-color tint as the actual ref badges — border/bg mixed from `--badge-color` at a comparable opacity to the active-ref fill (with regular `var(--text)` for contrast) — and dropdown rows should mirror the badge chrome (name left, icons right, lane-tinted fill), not read as a separate neutral menu ("it should have same lane color and icon possition on right side"). Confidence: 0.7

- Prefers the side-by-side diff's OLD and NEW panes to stay perfectly aligned — each aligned row must show identical content on both sides with accurate per-side line numbers. Reported a bug as "the OLD and NEW has different value" for @AGENTS.md L60-70 when a run of standalone additions (an insertion block) left the trailing context rows paired against the wrong old lines (OLD showed line 63 where NEW showed line 68). Treats this as a correctness bug to root-cause and fix, not a cosmetic one — the fix anchored in-hunk context rows to the hunk header's declared `oldStart`/`newStart` positions rather than relying on incremental index advancement. Confidence: 0.7
- For the side-by-side diff minimap, wants it on BOTH panes with each pane's markers derived from that pane's own changes only, colored strictly by that side's change kind: the OLD pane marks removals red, the NEW pane marks additions green, context rows stay dim, and rows that changed only on the other side leave a gap (no marker) rather than showing a neutral or mirrored marker ("better show minimap on both pane, but the marker is based on their changes"). Rejected the intermediate "paired edit" accent special-case: when a paired-edit row (old `-` + new `+` collapsed into one row) was painted green/accent on the OLD pane, the user reported it as a bug ("on store.ts, minimap OLD pane show green stripe. but the changes actually '-' type") — a row that is a removal on a pane must always read red there and an addition green, even when it's half of an old→new replacement (OLD shows `remove`, NEW shows `add`); the derived `edit` kind and its CSS rule were removed entirely. Extends the same principle as the two-pane content rule: each side renders its own data, never mirrored. Confidence: 0.8
- Reports bugs with the exact view/state that reproduces them and treats each diff surface as an independently-correct path: when the diff misalignment persisted after a fix, the user narrowed it to "it happen only at staged, on unstaged was normal" — the staged-vs-unstaged distinction is a diagnostic clue to trace which data path diverges (the staged view's old side was built from the wrong revision baseline — `HEAD^` instead of `HEAD` — mismatching the `git diff --cached` hunks), not a suggestion that one surface can stay broken. Every diff view (staged, unstaged, commit) must render from a baseline consistent with its hunks; a fix is not done until it holds on the specific surface the user named. Confidence: 0.7
  nder, making the component appear not to render at all. Confidence: 0.6
  cached` hunks), not a suggestion that one surface can stay broken. Every diff view (staged, unstaged, commit) must render from a baseline consistent with its hunks; a fix is not done until it holds on the specific surface the user named. Confidence: 0.7
  nder, making the component appear not to render at all. Confidence: 0.6
